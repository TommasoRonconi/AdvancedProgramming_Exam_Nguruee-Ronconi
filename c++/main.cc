#include <Tree.h>

int main( /* int argc, char * argv[] */ ) {

  // Tree< int, double >::Node N1;
  // if ( N1->parent )
  //   std::cout << "Blabla\n";

  
  // Tree< int, double > T1;
  // std::cout << *(T1.begin()) << std::endl;
  // auto it = T1.begin();
  // if (!it->)
  //   std::cout << "nullptr!\n";
  
  /* code */
  return 0;
}
